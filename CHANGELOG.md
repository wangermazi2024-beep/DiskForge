# Changelog

## v0.1.4 (Beta)

### 修复与优化

- 删除到回收站改用微软官方推荐的现代 IFileOperation 接口，替换已过时的 SHFileOperation：修复大文件夹（如几十万碎文件的缓存目录）删除时"进度跑完 100% 却静默中止、文件夹原封不动"的问题（旧 API 返回 0x78 源访问被拒且无任何提示）
- 删除成败最终以"目标是否真的消失"为准：API 报成功但文件还在时明确报错"部分文件被占用或访问被拒"，不再误报完成
- 删除失败原因（含 HRESULT 错误码）写入日志面板，便于排查具体是哪一步失败

## v0.1.3 (Beta)

### 修复与优化

- 删除到回收站不再静默执行：文件较多、删除耗时较长时显示 Windows 自带进度窗口，避免"看起来没反应"；同时移除自动重试（大文件夹每轮重试都要重新枚举几十秒，失败反复浪费时间），失败即提示并保留占用进程检测
- 迁移文件夹时如果包含符号链接/junction 子项，改为弹窗让用户选择继续或终止：继续则只重建链接、不重复复制其内容，一键还原时链接原样还原成链接（文件还原成文件）
- 文件夹迁移还原脚本命名调整：`DiskForgeRestoreLink_文件夹名_哈希_时间戳`（去掉了括号，避免手动在 PowerShell 里运行时被特殊字符干扰）

## v0.1.2 (Beta)

### 修复与优化

- 符号链接真实路径解析改用 Windows 官方 API（CreateFileW + GetFinalPathNameByHandleW），由内核一步解析整条链接链，不再有任何层数硬编码限制；断链/权限不足时直接明确报错
- 解析函数经二次评审微调：句柄生命周期用闭包收紧（任何提前退出都先于 CloseHandle），修正缓冲区扩容注释，并补充 Windows 内核"单路径最多 63 个 reparse 点"的系统级边界说明
- 一键还原脚本重构为 bat + ps1 + csv 三件套：bat 入口内容纯 ASCII（彻底杜绝中文代码页乱码导致的命令错乱），并自动通过 UAC 申请管理员权限；PowerShell 脚本（UTF-8 BOM）按 CSV 记录全自动还原全部条目，无需逐条手选
- 还原失败时提示"以管理员身份运行重试"，管理员也失败时指引打开 symlink_records.csv 按记录手动还原
- 还原成功后明确提示：先确认相关软件运行正常，再自行删除 DiskForge 里的存档数据释放空间
- 还原记录 CSV 支持多批累计并按链接去重，脚本固定放在真实数据所在目录
- 创建符号链接时的二次确认弹窗文案与还原脚本保持一致

## v0.1 (Beta)

首个公开发布的测试版本。

### 功能

- NTFS 主文件表（MFT）直读极速扫描，非管理员自动回退常规遍历，结果同样准确
- 磁盘空间树形全景：逻辑大小 / 物理大小双列，NTFS 压缩、稀疏文件、硬链接均正确处理
- 文件扩展名分类统计、跨盘重复文件查找（支持删除多余副本或替换为符号链接）
- 文件占用检测（可结束占用进程 / 停止占用服务）
- 大文件夹符号链接迁移（搬到其他盘、原位置留下"传送门"）
- 查找（Ctrl+F，支持通配符）与全盘搜索（支持正则表达式）
- 右键菜单全套操作：打开位置、删除到回收站、检测占用、创建符号链接、重新扫描等
- 扫描结果导出 CSV（Excel 可直接打开）
- 多分区排队扫描、多标签页、诊断日志

### 说明

- 本版本为测试版（Beta），功能与界面仍会持续调整
- 免安装单文件软件，不联网、不上传任何数据
