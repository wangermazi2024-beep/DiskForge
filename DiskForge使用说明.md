# DiskForge 使用说明

**版本：v0.1.0 Beta　|　免费软件（MIT 许可证）　|　作者：WMS**

DiskForge 是一款免费的 Windows 磁盘空间与文件分析器。它只有一个 exe 文件，**不需要安装**，下载后双击就能用。它能帮你快速搞清楚"磁盘空间都被什么东西吃了"，还能帮你安全地清理重复文件、把大文件夹搬到别的盘、查出"文件删不掉是因为被哪个程序占用"。

---

## 一、第一次使用

### 1. 启动

双击 `DiskForge.exe` 即可打开，无需安装、不写注册表。

- **建议以管理员身份运行**：软件能直接读取 NTFS 磁盘的 MFT 主文件表，扫描速度快非常多。如果不是管理员身份启动，点顶栏右侧黄色的 **「⚡ 以管理员身份运行」** 按钮，软件会自动用管理员权限重启。
- 非管理员也能用（自动改用普通方式遍历文件），只是扫描速度慢一些。
- 首次启动会弹出赞助窗口：可以扫码请作者喝杯咖啡，也可以点 **「下次一定」**（下次启动还会问）或等倒计时结束后点 **「不再提醒」**（以后不再弹出）。

### 2. 选择要扫描的内容

启动后第一步是选择扫描对象：

- **固定磁盘分区**：勾选 C 盘、D 盘等，可以多选；
- **自定义目录**：点「📁 浏览文件夹…」添加任意文件夹（比如只扫某个巨大的资料库目录）。

选好后点蓝色的 **「开始扫描」** 按钮。扫描过程中顶栏会显示进度（"正在扫描… 已发现 N 项"）。

### 3. 看结果

扫描完成后，主界面分为三块：

| 区域 | 位置 | 内容 |
|------|------|------|
| 顶栏菜单 | 最上方 | 所有功能入口（文件 / 视图 / 分析 / 查找 / 关于） |
| 左侧栏 | 左边 | 磁盘概览圆环 + 分类统计 + 两个分析按钮 |
| 主列表 | 中间大区域 | 树形文件列表，点开文件夹层层展开 |

---

## 二、界面与功能详解

### 1. 左侧栏

- **磁盘概览**：一个圆环显示当前选中分区"已用空间"的百分比，下面标注"已用多少 / 总共多少"。
- **分类统计**：视频、图片、文档、压缩包、程序等大类各占了多少空间，用彩色小条显示占比。
- **两个快捷按钮**：
  - 「🗐 文件扩展名分类」：按具体扩展名（.mp4 / .jpg / …）统计占用；
  - 「🔍 重复文件查找」：找出内容完全相同的重复文件。

### 2. 主列表（树形）

- **展开/折叠**：点文件夹前面的小箭头逐层展开。
- **排序**：点击表头（名称、大小、修改时间等）排序，再点一次切换升/降序。
- **过滤框**：顶部输入框可以直接按名称过滤，支持通配符，例如输入 `*.mp4` 只看视频文件。
- **常用列的含义**：
  - **逻辑大小**：文件本身的字节数（相当于"标称大小"）；
  - **物理大小**：实际占用的磁盘空间（压缩文件、稀疏文件会比逻辑大小小）；
  - **总占比**：这一项占整个分区空间的百分比；
  - **父占比**：这一项占它所在文件夹的比例（一眼看出哪个子文件夹最占地方）；
  - **重解析点**：标着"是"的行说明它本身是符号链接 / junction / 挂载点——它只是指向别的位置的"快捷方式"，不实际占用这里显示的空间。

### 3. 右键菜单（对任意文件/文件夹）

在主列表里右键任何一项，可以：

| 菜单项 | 作用 |
|--------|------|
| 📂 打开所在文件夹 / 在资源管理器中打开 | 跳转到该文件所在的 Windows 文件夹 |
| 📋 复制名称 / 📋 复制路径 | 把名称或完整路径复制到剪贴板 |
| ℹ 属性 | 打开 Windows 自带的属性对话框 |
| 🔄 重新扫描 | 只刷新这一个分区/目录，用最新结果原地替换，不影响其它分区 |
| ✖ 从列表移除 | 只是不在这个列表里显示了，**不会删除磁盘上的任何文件**，重新扫描就能加回来 |
| 🗐 文件扩展名分类 | 只针对这一个分区做扩展名统计 |
| 🔍 重复文件查找 | 只针对这一个分区找重复文件 |
| 🔍 检测占用 | 查出是哪个程序/服务正占用着这个文件（删不掉的时候用） |
| 🔗 创建符号链接 / 迁移到其他盘 | 把文件搬到另一个盘，原地留一个几乎不占空间的"传送门" |
| 🗑 删除到回收站 | 删除进回收站，**可以恢复**；删除文件夹会连里面所有内容一起删，请看清楚再点 |

### 4. 顶栏菜单功能

**「文件」菜单**

- **添加扫描…**：再扫一个分区或目录（同一个窗口可同时管理多个分区）。
- **导出 CSV…**：把扫描结果导出成表格文件（每个分区一个 CSV），可以用 Excel 打开，字段包含路径、大小、各种时间、属性等。
- **📑 复制列表**：把当前数据复制成一个独立的新标签页，方便对着同一份数据的不同状态分开查看对比，不影响主列表。

**「视图」菜单**

- **显示全部信息（含元数据文件）**：勾上后会显示 NTFS 系统内部的元数据文件（如 $MFT），普通用户一般不需要看，默认隐藏。

**「分析」菜单**

- **地毯 文件扩展名分类…（全部分区）**：把所有已扫描分区混在一起按扩展名统计；只想看单个分区就右键那个分区选同名菜单。
- **🔍 查找重复文件…（全部分区）**：跨盘找重复文件（比如 C 盘和 D 盘各存了一份同样的安装包也能找出来）。

**「查找」菜单**

- **🔎 查找… (Ctrl+F)**：在当前标签页里**定位**某一项——会自动展开、选中并滚动过去，不改变列表内容，纯粹帮你跳过去。
- **🔍 搜索文件…**：打开一个搜索标签页，在整个扫描结果里按名称搜文件，**支持通配符和正则表达式**，结果可以排序，也能直接右键删除或创建符号链接。

**「关于」菜单**

- 版本、版权与许可信息，以及赞助入口。

---

## 三、几个重点功能的用法

### 1. 重复文件查找（省空间利器）

1. 点「分析」→「🔍 查找重复文件…（全部分区）」，或右键某个分区；
2. 软件先按"文件大小相同"找出候选，再**逐字节比较内容**确认（不是靠哈希碰巧一样，结果可靠）；
3. 结果按组显示：同一组里的文件内容完全一致，显示每组"预计可省"多少空间；
4. 对重复组可以执行"替换为符号链接"：保留一份实体文件，其它副本变成指向它的链接，**文件看起来还在原位能用，但磁盘空间只占一份**。

### 2. 检测占用（"文件删不掉"救星）

删除文件时提示"正在被其它程序使用"？右键它选「🔍 检测占用」：

- 软件会列出具体是哪些进程/服务占用着它；
- 可以「⚡ 一键结束全部」，也可以单独结束某个进程 / 停止某个服务；
- 如果第一层没查出来，可以「▶ 继续往下查一层」——软件用"重命名探测"一层一层往下试，直到查到最深层；
- 查完显示「✓ 没有检测到占用」就可以放心删除或创建符号链接了；
- 如果查到最深层也没发现占用，那多半是权限不足或系统保护目录，不是被占用。

### 3. 创建符号链接 / 迁移到其他盘（给 C 盘减肥）

比如 C 盘有个 20GB 的游戏/资料文件夹想搬到 D 盘，又不希望程序找不到它：

1. 右键该文件夹 →「🔗 创建符号链接 / 迁移到其他盘」；
2. 选择目标位置，软件会把内容搬过去，原地留下一个符号链接；
3. 之后所有程序访问原路径时会被自动"传送"到新位置，**看起来一切照旧，C 盘空间却省下来了**。
4. 注意：本身已经是符号链接/junction 的项目不支持再次创建或迁移。

### 4. 搜索标签页

需要按名字海里捞针时，用「查找」→「🔍 搜索文件…」：

- 输入名称关键词、通配符（如 `*.pid`）或正则表达式；
- 结果列表可按任意列排序；
- 右键结果可直接「删除到回收站」或「创建符号链接」。

---

## 四、常见问题（FAQ）

**Q：扫描很慢？**
点顶栏黄色「⚡ 以管理员身份运行」按钮重启软件，管理员模式下走 MFT 直读，速度会快很多。

**Q：「从列表移除」会删我的文件吗？**
不会。它只是把这一项从显示列表里拿掉，磁盘上一个字节都没动，重新扫描就能加回来。

**Q：删除到回收站的文件还能找回来吗？**
能。所有删除都是进回收站的，去 Windows 回收站还原即可。

**Q：什么是"重解析点"？**
符号链接、junction、挂载点的统称，相当于指向别处的"传送门"，本身几乎不占空间。

**Q：CSV 导出失败？**
详情写在日志里，按日志提示排查（常见原因是目标文件夹没有写入权限）。

**Q：扫描过程中能再添加其它扫描吗？**
同一个时间只允许一个扫描任务在进行中，等当前扫描完成后再添加。

---

## 五、关于本软件

- **作者**：WMS　**邮箱**：wumingshifn@gmail.com
- **许可**：免费软件，基于 MIT 许可证开源发布
- 如果这个软件帮到了你，欢迎在「关于」窗口扫码赞助作者（金额随意，不强求）

> 温馨提示：本软件只做"分析"与"你主动触发的操作"，不会在后台偷偷改动任何文件。涉及删除、迁移的操作都有明确确认，且删除一律进回收站，可放心使用。
